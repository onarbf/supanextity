
//Its important to have this for every route. The name needs to match the name of the page in the translations .json files because that is what we are going to use to get the correct Metadata
export const paths: Record<string,string>  = {
    '/': 'Home',
    '/about' : 'About'
}